Your project is already committed locally on branch `main`.

One-time: create a new empty repo on GitHub
  • github.com → New repository
  • Do NOT add README, .gitignore, or license (avoids merge noise)

Then in PowerShell, from this folder:

  cd "c:\Users\DRMLWhite\Downloads\oracle_of_olympus_printables"
  git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
  git push -u origin main

If `origin` already exists from a failed try:

  git remote remove origin
  git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
  git push -u origin main

GitHub will ask you to sign in (browser or token). After push, connect Vercel to that repo.
